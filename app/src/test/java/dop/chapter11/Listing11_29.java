package dop.chapter11;

import org.junit.jupiter.api.Test;

public class Listing11_29 {
    /**
     * ───────────────────────────────────────────────────────
     * Listing 11.29
     * ───────────────────────────────────────────────────────
     * Traditionally, we might tackle it like this
     * ───────────────────────────────────────────────────────
     */
    /*
    @BeforEach
    void setup() {
       mock(...)
       mock(...)
       new Synchronizer(...);
    }
    @Test
    void theFirstBranchWhereTheMirrorHasNothingYetAndSoWeCopy() {
      when(...).then(...)
      when(...).then(...)
      ...
      assert(...)
    }

    @Test
    void theSecondWhereTheTwoAreInSyncAndThereforWeDoNotCopyAnything() {
      when(...).then(...)
      when(...).then(...)
      ...
      assert(...)
    }
    // and so on...
    */
}
